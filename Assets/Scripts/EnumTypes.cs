namespace EnumTypes
{
    public enum GoodsType {
        Fish, Can, Rep
    }
    public enum FoodType {
        Food, Drink
    }
    public enum Rarity {
        Common, Rare, Unique, Hidden, Special
    }
}